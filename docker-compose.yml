version: "3.8"

services:
    nginx:
        build:
            context: ./nginx
            dockerfile: Dockerfile
        ports:
          - "80:80"
          - "443:443"
        depends_on:
          - django
          - nextjs
   
    django:
        build:
            context: ./django
            dockerfile: Dockerfile

    nextjs:
        build:
            context: ./frontend
            dockerfile: Dockerfile
