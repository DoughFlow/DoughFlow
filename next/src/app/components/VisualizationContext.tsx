import React from 'react';

type Props = {
    inp: string;
}

const VisualizationContext = ({ inp }: Props) => {

    return (
      <div className='border-2 text-5xl'>
        beast component
      </div>
    );
};

export default VisualizationContext;
