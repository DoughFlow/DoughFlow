import { smaDataPoint } from "../fetchData";

export const smSmaSvg = (data: smaDataPoint[], height: number, width: number): string => {
    return "";
};

export const smaSvg = (data: smaDataPoint[], height: number, width: number): string => {
    return "";
};

