import { rsiDataPoint } from "../fetchData";

export const smRsiSvg = (data: rsiDataPoint[], height: number, width: number): string => {
    return "";
};

export const rsiSvg = (data: rsiDataPoint[], height: number, width: number): string => {
    return "";
};

