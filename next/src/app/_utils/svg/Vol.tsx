import { volDataPoint } from "../fetchData";

export const smVolSvg = (data: volDataPoint[], height: number, width: number): string => {
    return "";
};

export const volSvg = (data: volDataPoint[], height: number, width: number): string => {
    return "";
};

